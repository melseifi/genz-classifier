from setuptools import setup, find_packages
setup(name="genz-classifier",
      version="1.0",
      author= "Mohamed Elseifi",
      packages=find_packages(),
      python_requires=">=3.6",
      install_requires=["numpy >= 2.4",],)
